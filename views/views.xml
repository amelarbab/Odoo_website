<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="lead_website.list">
      <field name="name">lead_website list</field>
      <field name="model">lead_website.lead_website</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="lead_website.action_window">
      <field name="name">lead_website window</field>
      <field name="res_model">lead_website.lead_website</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="lead_website.action_server">
      <field name="name">lead_website server</field>
      <field name="model_id" ref="model_lead_website_lead_website"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="lead_website" id="lead_website.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="lead_website.menu_1" parent="lead_website.menu_root"/>
    <menuitem name="Menu 2" id="lead_website.menu_2" parent="lead_website.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="lead_website.menu_1_list" parent="lead_website.menu_1"
              action="lead_website.action_window"/>
    <menuitem name="Server to list" id="lead_website" parent="lead_website.menu_2"
              action="lead_website.action_server"/>
    -->
  </data>
</odoo>